import React from "react";
import './LoaderComponent.css'; // Optional: If you want to style your loader

const LoaderComponent = () => {
  return (
    <div className="loader">
      <p>Loading...</p>
    </div>
  );
};

export default LoaderComponent;
